@{
    Layout = "~/Views/Shared/userLayout.cshtml";
}
@model HospitalManagementSystem.Models.PatientLogin


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Patient Portal - Login</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet">

    <style>
        body, html {
            height: 100%;
            margin: 0;
            background: url('/img/hospital.jpg') no-repeat center center fixed;
            background-size: cover;
        }

        .login-card {
            max-width: 400px;
            background: rgba(255, 255, 255, 0.95);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.2);
        }

        .login-container {
            height: 100vh;
        }

        .logo {
            width: 150px;
            margin-bottom: 10px;
        }

        .support-text {
            font-size: 0.9rem;
            color: #555;
        }

        .form-control::placeholder {
            font-style: italic;
        }

        .register-link {
            font-size: 0.9rem;
            color: #007bff;
            text-decoration: none;
        }

            .register-link:hover {
                text-decoration: underline;
            }
    </style>
</head>
<body>
    <div class="d-flex align-items-center justify-content-center login-container">
        <div class="login-card text-center">
            <a class="navbar-brand" href="#">
                <h3>
                    <b>
                        🏥
                        CureLife Hospital
                    </b>
                </h3>
            </a>
            <h4 class="mb-3">Patient Portal</h4>

            @using (Html.BeginForm("PatientLogin", "Patient", FormMethod.Post))
            {
                @Html.AntiForgeryToken()

                <div class="form-group mb-3">
                    @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder = "Enter your Email" })
                    @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                </div>
                <div class="form-group mb-3">
                    @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "Enter your Password" })
                    @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                </div>          
                <!-- Add Forgot Password link here -->
                <div class="text-end mb-3">
                    <a href="@Url.Action("ForgotPassword", "Patient")" class="register-link">Forgot Password?</a>
                </div>

                <button type="submit" class="btn btn-primary w-100">
                    Sign In
                </button>
            }

            <p class="mt-3 support-text">
                Having Trouble in Sign In? Kindly contact us on<br />
                <strong>+91-261-7161111</strong>
            </p>

            <p class="mt-3">
                <span>Don't have an account?</span>
                <a href="@Url.Action("PatientRegister", "Patient")" class="register-link">Register Now</a>
            </p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
