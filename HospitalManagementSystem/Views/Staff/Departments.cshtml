@{
    Layout = "~/Views/Shared/admin.cshtml";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Staff Departments</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

    <!-- FontAwesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

    <style>
        body {
            background: #f4f7f6;
            font-family: Arial, sans-serif;
        }

        .container {
            margin-top: 50px;
        }

        .card {
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }

        .card-header {
            background-color: #007bff;
            color: white;
            text-align: center;
            font-size: 22px;
            font-weight: bold;
        }

        .form-control {
            border-radius: 5px;
        }

        .btn {
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="col-md-2" style="margin-left:40px;">
        <a href="@Url.Action("DisplayDepartments","Staff")" class="btn btn-primary btn-sm px-3 py-2" style="font-size: 14px;">
            <i class="fa-solid fa-list"></i> Departments List
        </a>
    </div>
    <div class="container">
        <div class="card p-3">
            <div class="card-header">
                <i class="fa-solid fa-building"></i> Staff Departments
            </div>

            <div class="card-body">
                <form id="departmentForm">
                    <div class="mb-3">
                        <label class="form-label"><i class="fa-solid fa-building"></i> Department Name</label>
                        <input type="text" class="form-control" id="department_name" placeholder="Enter Department Name" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label"><i class="fa-solid fa-user-tie"></i> Department Head</label>
                        <select class="form-control" id="department_head">
                            <option value="">Select Department Head</option>
                            <!-- Fetch Staff Members from Database -->
                        </select>
                    </div>

                    <div class="row mt-3">
                        <div class="col-md-6">
                            <button type="submit" class="btn btn-success">
                                <i class="fa-solid fa-floppy-disk"></i> Save Department
                            </button>
                        </div>
                        <div class="col-md-6">
                            <button type="reset" class="btn btn-danger">
                                <i class="fa-solid fa-eraser"></i> Reset
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>

      
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- JavaScript for Handling Form Submission -->
    <script>
        // Handle form submission
        document.getElementById("departmentForm").addEventListener("submit", function(event) {
            event.preventDefault();

            // Get values from form
            let department_name = document.getElementById("department_name").value;
            let department_head = document.getElementById("department_head").value;

            // Insert into table
            let table = document.getElementById("departmentTable");
            let newRow = table.insertRow();
            newRow.innerHTML = `
                <td>#</td>
                <td>${department_name}</td>
                <td>${department_head || 'Not Assigned'}</td>
            `;

            document.getElementById("departmentForm").reset();
        });
    </script>

</body>
</html>
